#!/bin/bash
set -eu

VERSION=0.9.0
# from https://releases.hashicorp.com/vault/0.9.0/vault_0.9.0_linux_amd64.zip
cd vault
unzip vault_${VERSION}_linux_amd64.zip
mkdir -p ${BOSH_INSTALL_TARGET}/bin
cp vault ${BOSH_INSTALL_TARGET}/bin
